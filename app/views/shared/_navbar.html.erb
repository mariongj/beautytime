<nav class="navbar navbar-default navbar-wagon" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">
        <%= image_tag "logo_large.png" %>
      </a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
              <%= image_tag (current_user.picture.present? ? current_user.picture.url : "penelope.jpg"), class: "img-avatar" %>
              Espace personnel <span class="caret"></span>
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to bookings_user_path(current_user) do %>
                  <i class="fa fa-home"></i> Mes rendez-vous
                <% end %>
              </li>
              <li>
                <%= link_to institutes_user_path(current_user) do %>
                  <i class="fa fa-home"></i> Mes instituts
                <% end %>
              </li>
              <li>
                <%= link_to reviews_path do %>
                  <i class="fa fa-home"></i> Mes avis
                <% end %>
              </li>
              <li>
                <%= link_to user_path(current_user) do %>
                  <i class="fa fa-home"></i> Mon profil
                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t ".sign_out", default: "Se déconnecter" %>
                <% end %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <%= link_to t(".sign_up", default: "Sign up"), new_user_registration_path %>
          </li>
          <li>
            <%= link_to t(".sign_in", default: "Sign in"), new_user_session_path %>
          </li>
          <li>
            <%= link_to "Connect with Facebook", user_omniauth_authorize_path(:facebook) %>
          </li>
        <% end %>

        <li>
          <%= link_to t(".top_call_to_action", default: "NOUVEAU RENDEZ-VOUS"), root_path, class: "btn" %>
        </li>
        <li>
          <% if user_signed_in? %>
            <%= link_to business_institutes_path do%>
             <i class="fa fa-envelope-o"></i> Espace Pro
            <% end %>
          <% else %>
            <%= link_to new_user_session_path do %>
            <i class="fa fa-envelope-o"></i> Espace Pro
            <% end %>
          <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
